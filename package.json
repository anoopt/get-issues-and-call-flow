{
  "name": "github_actions_get_issues",
  "version": "1.0.0",
  "description": "GitHub Action to get issues based on label",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anoop Tatti",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/github": "^2.0.0",
    "fetch": "^1.1.0"
  }
}
